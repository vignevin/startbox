% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graphs.R
\name{plot_meteo}
\alias{plot_meteo}
\title{Plot ombrothermic meteo chart}
\usage{
plot_meteo(
  self,
  start_day = NULL,
  end_day = NULL,
  rain_date_labels = TRUE,
  ppp_marks = TRUE,
  obs_marks = TRUE
)
}
\arguments{
\item{self}{A \code{user_data} R6 object containing a \code{meteo} data.frame}

\item{start_day}{Optional start date for filtering the data (format \code{YYYY-MM-DD} or \code{DD/MM/YYYY}).}

\item{end_day}{Optional end date for filtering the data.}

\item{rain_date_labels}{Logical, default \code{TRUE}. If \code{TRUE}, adds date labels above rainfall bars where \code{rain_mm > 5}.}

\item{ppp_marks}{Logical, default \code{TRUE}. If \code{TRUE}, draws treatment markers using unique dates found in \code{self$metadata$ppp$p_app_date} within the filtered range.}

\item{obs_marks}{Logical, default \code{TRUE}. If \code{TRUE}, draws observation markers using unique \code{observation_date} values merged from all data frames in \code{self$obs_data} within the filtered range.}
}
\value{
A \code{ggplot} object representing the ombrothermic chart.
The used data (after cleaning and filtering) is attached as an attribute \code{"data_used"}.
}
\description{
This function generates an ombrothermic plot from meteo data stored in the \code{self$meteo} attribute of the \code{user_data} object.
It displays daily rainfall (in mm) as bars and daily minimum, mean, and maximum temperatures (in °C) as lines.

The x-axis shows the months (horizontal labels) and the specific dates where
rainfall exceeded 5 mm (vertical labels aligned below the axis).
}
\examples{
\dontrun{
# example code
# where mydata is the object into which the user_data class has been loaded
mydata <- user_data$new(trial_file = "path to our trial file")

plot_meteo(mydata,
   start_day = "01/04/2025",
   end_day = "30/07/2025",
   rain_date_labels = FALSE)
}

}
