% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inOut.R
\name{import_pom_csv}
\alias{import_pom_csv}
\title{Import meteo data from a EPICURE POM csv file}
\usage{
import_pom_csv(self, filepath, skip_forecast = TRUE, update = c("new", "all"))
}
\arguments{
\item{self}{An \code{user_data} R6 object in which to import the meteo}

\item{filepath}{File path of the csv file to be imported. This file must be a csv file exported from Epicure (IFV Information system)}

\item{skip_forecast}{Logical. If \code{TRUE} (default), the first 14 data rows are removed before processing (commonly forecast values).}

\item{update}{Character. Determines how to update existing meteorological data: \code{"new"} (default) or \code{"all"}.}
}
\value{
Updates \code{self$meteo} with the imported data, update also the log. Returns \code{invisible(self$obs_data)}.
}
\description{
This function imports meteorological data from a CSV file exported from the EPICURE Information System.
The data are standardized before being added to \code{self$meteo}.

If meteorological data already exist in \code{self$meteo}, the \code{update} parameter allows the user to:
\itemize{
\item \code{"new"}: append only the data more recent than the last \code{meteo_datetime} in \code{self$meteo}.
\item \code{"all"}: replace all existing data with the new data.
}

The function also updates the internal log (traceability).
}
\details{
The function automatically detects new rows based on the \code{meteo_datetime} column.
If \code{update = "new"} but \code{self$meteo} is empty, all data are imported.
A message is displayed indicating the number of rows imported.
}
\examples{
\dontrun{
# Import new meteorological data into an existing user_data object
import_pom_csv(self = my_user_data, filepath = "meteo_2025.csv", update = "new")

# Replace all existing meteorological data
import_pom_csv(self = my_user_data, filepath = "meteo_2025.csv", update = "all")
}

}
